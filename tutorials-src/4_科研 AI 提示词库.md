---
title: "科研 AI 提示词库：代码调试 / 图表规范 / 论文写作全场景模板"
date: 2026-07-18
summary: "科研 AI 提示词库：代码调试 / 图表规范 / 论文写作全场景模板. 整理自 Gemini/Claude/ChatGPT 的科研提示词合集，涵盖 R/Python 代码续写与调试、单细胞分析、图表美化、Nature 风格论文写作等场景，可直接复用。"
type: tutorial
tags: ["Jian Wu", "jwtools", "AI提示词",'科研效率', '代码调试']
#cover: "https://github.com/JackNg88/jwtools/raw/main/man/figures/logo.png"
cover: "https://jackng88.github.io/assets/img/logos/Jian_Wu_logo.png"
---

完整代码? 从最开始处 加上详细备注信息

我的旧版[];但现在完全参考这重写[]

参考 Myeloid 版本框架，完整迁移到 所有细胞 谱系XX


同理, 增加: 各个的转座子等详细信息? 例如: 是哪些TE?

---

# (0): Chat AI

# AI 提示词库扩展推荐
## 基于你现有习惯的分类整理 + 常用高频模板补充

看你现有的几条提示词，核心模式是：**"续写完整代码 + 参考框架迁移 + 详细注释 + 补充某类详情"**。这是非常典型的"大型多Section流程化脚本迭代开发"场景。我按你的实际工作流（R/Seurat单细胞 + 转座子分析 + 图表生成 + 论文写作）帮你扩展一套更系统的提示词库，可以直接追加到你的 `.sh` 文件里。

---

## 📌 分类一：代码续写 / 完整版（你已有，补充变体）

```bash
# ===== 续写类 =====
完整代码? 从最开始处, 加上详细备注信息
剩下的, 完整版?
接下来的, 完整版? 从Section XX开始
中间省略的部分, 请补全, 不要用注释代替代码
这一段被截断了, 请从断点处继续, 不要重复已给出的内容
完整代码, 但只给我修改的Section, 其余用 [代码与上一轮一致] 标注
```

## 📌 分类二：代码迁移 / 框架复用（你已有，补充变体）

```bash
# ===== 迁移类 =====
参考 [X] 版本框架, 完整迁移到 [Y] 场景, 保留原有的详细注释风格
我的旧版 []; 但现在完全参考这重写 []
这个函数/Section在新场景下, 生物学假设是否仍然成立? 若不成立, 应该怎么改?
把 Myeloid 单一谱系的方法学, 迁移到 10个细胞类型的场景, 哪些Section需要重新设计逻辑(而非简单替换变量名)?
保持函数签名不变, 只替换内部的细胞类型判断逻辑
```

## 📌 分类三：代码调试 / 报错修复（你目前较少，建议补充）

```bash
# ===== 调试类 =====
报错信息如下: [粘贴报错], 请定位具体是哪一行/哪个函数导致的, 并给出修复后的完整函数
这段代码逻辑上没问题, 但结果不符合预期(应该是XX, 实际是YY), 请帮我诊断可能的原因
帮我加一段诊断代码, 在跑正式流程前先检查 [某个前提条件, 如样本量/年龄跨度] 是否满足
这个函数在小数据集上正常, 但在27万细胞规模下报错/卡住/内存溢出, 如何优化?
这里为什么要用 unname()/GetAssayData(layer=...)? Seurat v5和v4的API差异是什么?
```

## 📌 分类四：代码优化 / 重构（建议新增）

```bash
# ===== 优化类 =====
这段代码能否向量化以提升速度? 目前是for循环逐个celltype处理
这几个函数有重复逻辑, 能否抽象成一个通用函数, 用参数控制差异?
内存优化: 这一步是否需要先释放某些大对象(如scale.data/counts矩阵)再继续?
这个Section的输出文件命名不够规范, 帮我统一成 [数字前缀]_[FigureID]_[内容]_[转座子].csv 的格式
帮我加上运行时间统计(tictoc或Sys.time()), 定位哪个Section最耗时
```

## 📌 分类五：可视化 / 绘图规范类（你之前问过dotplot/heatmap，可固化成模板）

```bash
# ===== 绘图类 =====
完全参考paper的[X]图风格, 但把数据换成我的; 图例/配色/坐标轴标签需要说明理由
gene用绿色, TE用紫色; 同一个cell type的feature用虚线圈出来; 左边加cell type标签框
这个图对于[N]个feature/celltype来说太拥挤了, 帮我设计一个可读性更好的替代方案(如分面/分页/Top N筛选)
根据生成的图, 帮我判断哪几个TE/celltype的结果不可靠(样本量太小/趋势不一致), 应该标注还是剔除?
这张图要放进论文Figure2, 请按Nature/Cell风格的图注(figure legend)格式帮我写一段
raster.dpi保持默认值, 不要用我之前设的全局变量
```

## 📌 分类六：生物学机制解读 / 假说构建（你的核心研究需求）

```bash
# ===== 机制解读类 =====
这个结果(如ERV burden在O/Y显著升高)在衰老/TE去抑制机制上, 可能对应哪些已知通路(cGAS-STING/KRAB-ZNF/SASP)?
这几个转座子家族(给出家族名单)在文献中是否有已知的顺式调控/免疫激活功能?
帮我设计一个实验/分析, 用于验证"TRIM28/SETDB1下降 → ERV解除抑制"这一假说的因果性(而非仅相关性)
这个跨cell type一致性结果, 支持"系统性衰老"还是"细胞类型特异性衰老"? 论证逻辑是什么?
这个发现和IPF/COVID-19/PH中ERV激活的报道相比, 有什么共性和特异性?
```

## 📌 分类七：论文写作 / 结果汇报（英文模板，你之前也用过）

```bash
# ===== 论文写作类 =====
把这段分析结果, 写成一段Methods, 参照Nature Genetics格式, 突出统计学方法选择的理由
把这几个图的结果, 整合写一段Results, 逻辑上从"是什么"到"为什么"到"意味着什么"
这个方法学替代(如GAM替代Slingshot)的理由, 帮我写一段Reviewer可能会问的Q&A预演
用一句话总结这个Figure的核心发现, 作为摘要/graphical abstract的候选文字
这几个候选转座子, 如果要写进Discussion的"局限性"部分, 应该如何措辞更严谨(避免过度解读)?
```

## 📌 分类八：数据诊断 / QC（大规模单细胞数据的常见痛点）

```bash
# ===== 数据诊断类 =====
帮我写一段诊断代码, 检查每个celltype x AgeGroup组合的样本量分布, 标出<4的组合
这一步为什么结果是NULL/空? 帮我拆解每个过滤条件, 分别打印通过/未通过的数量
这个GAM/KW检验的显著结果数量异常少, 帮我诊断是"统计不显著"还是"单调性/一致性条件"卡住了
帮我检查这个基因/TE名称在assay中是否存在, 若不存在, 可能的原因(命名不一致/大小写/版本差异)是什么?
这批样本的年龄分布是否足够支撑 s(age, k=4) 的样条自由度? 帮我打印每个Lineage的unique age数量
```

## 📌 分类九：版本管理 / 流程整理（你之前也问过.gitignore/脚本管理）

```bash
# ===== 版本管理类 =====
帮我把这个脚本按Section拆分成独立的.R文件, 并生成一个master脚本按顺序source它们
帮我写一个commit message, 总结这次修改的核心内容(生物学假设变化 + 技术实现变化)
这个项目的多个子分析(Myeloid/AllCell/Epi)之间, 哪些函数是完全通用的? 帮我抽取成shared_functions.R
帮我生成一份README, 说明这个脚本的输入/输出/依赖包/运行顺序
```

## 📌 分类十：元提示词（Prompt about Prompts，帮你更精准地问）

```bash
# ===== 元提示词类 =====
在回答前, 先确认你理解的[某个术语/需求]是否和我说的一致, 如有歧义请先反问
如果这个需求有多种实现方式, 请先列出2-3种方案的优缺点, 我选定后你再写完整代码
不要假设我要全部重写, 先告诉我具体哪几行/哪个函数需要改动
这个改动是否会影响其他Section复用的中间对象(如feature_sets/immune.combined的meta.data)? 请先检查依赖关系
```

---

## 🎯 针对你研究方向的专属高频模板（转座子/衰老/多组学整合）

```bash
# ===== 领域专属类 =====
这个TE family在hg38 RepeatMasker注释中的repClass/repFamily是什么? 是否有已知的年龄相关文献?
帮我设计一个soloTE/scTE的QC步骤, 排除mappability过低导致的假阳性DE-TE
这个ERV位点是否与eQTL数据库(如GTEx)中的已知调控变异重叠?
把这个lung aging的发现, 和heart/PH ERVAtlas项目的框架做一次交叉验证的可行性评估
这几个候选ERV, 是否适合做空间转录组(Spatial IPF-ERV)层面的验证? 需要哪些先验假设?
```

---

## 💡 使用建议

1. **按场景分文件管理**：你现在把所有提示词堆在一个 `.sh` 里，建议按上面10个类别拆成多个文件（如 `prompts_debug.sh`, `prompts_bio.sh`, `prompts_writing.sh`），用 `#` 加分类标题，方便快速定位。

2. **高频模板做成"填空式"**：比如把 "参考 [X] 版本框架, 完整迁移到 [Y] 场景" 做成可替换的模板，减少每次重新组织语言的成本。

3. **建议新增"追问链"模板**：你现在的提示词多是单轮独立指令，但像本次"Section 12/33 完整版"这种连续追问，可以固化成一个标准追问链：
   ```
   Step1: 参考[X]完整迁移到[Y]
   Step2: 剩下的完整版?
   Step3: [某个Section]完整版? (指定编号)
   Step4: 报错如下, 请修复
   ```

需要我把这10类 + 领域专属类，整理成一个可以直接保存为 `.sh`/`.md` 的完整文件（带目录/编号），方便你复制粘贴管理吗？

---
---

# (2) Claude
根据您这份文件里已有的提示词风格（简短、直击核心、可复用于代码/图注/数据分析多场景），结合您平时的实际工作流（Seurat单细胞、转座子分析、图表复用、manuscript撰写），我给您整理一份补充清单，按场景分类，风格与您现有的保持一致：

## 一、代码类

```
完整代码？从最开始处，加上详细备注信息

我的旧版[];但现在完全参考这重写[]

参考Myeloid版本框架，完整迁移到所有细胞谱系XX

剩下的完整版？

同理，增加：各个的转座子等详细信息？例如：是哪些TE?

---补充---

这段代码报错了，帮我定位问题并给出修复后的完整版本，标注修改了哪一行

把这个函数参数化，让celltype/age_group/output_path都可以外部传入

这段代码跑内存太大了，帮我优化成memory-safe版本（比如用subsampling或分批处理）

帮我把这几个重复的section合并成一个通用函数，输入不同参数即可复用

检查这段代码里有没有硬编码路径/变量，帮我列出来并改成变量形式

这个图的配色/顺序/字体大小，完全按照XX图的风格统一

这段代码在Seurat v5下是否兼容？如果有v4遗留写法，帮我更新
```

## 二、图注/写作类（Nature风格manuscript）

```
完全参考这个图注格式重写：[参考文本]

这段Results，参考这篇文献的写作逻辑和句式，改写成我的数据版本

这段话里有没有因果语言（"regulates"这类），在Figure 7功能验证前，帮我改成relational表述

这段摘要，按照LEGO五段式结构（背景-问题-方法-发现-意义）重新组织

这个Discussion，按六段式结构（总结-机制-局限-对比-意义-展望）帮我扩写

这个title，给我列出7种不同类型的候选（描述型/悬念型/发现型等）

用reviewer的12条checklist标准，帮我审视这段Results有没有逻辑漏洞
```

## 三、生信分析类（专属于您的转座子 workflow）

```
同理，增加：各个的转座子等详细信息？例如：是哪些TE?

---补充---

这个分析，帮我加一版只针对HERV(541个)的子集版本

这段DEG分析，帮我拆成all genes / TE-only / ERV-only三层，输出格式统一

这个热图，行按cell type分组，列按TE family分组，加上注释色条

这个基因列表，帮我核对哪些是真实TE（用scTE命名表交叉验证），哪些可能是普通基因误标

这段代码统计的是TE位点数还是TE-neighboring基因数？帮我在注释里写清楚

这个模块评分（AddModuleScore），换成signed-log公式处理负值，避免log(负数)报错

这个GO富集，帮我做一版"至少2个cell type共享的通路"筛选逻辑

这段分析结果，帮我做Y vs M vs O三组两两比较（O/Y, M/Y, O/M）而不只是Y vs O
```

## 四、跨脚本迁移/批处理类

```
参考Myeloid版本框架，完整迁移到所有细胞谱系（Lymphoid/Epithelial/Stromal）

这个for循环，改成purrr::map或sapply的向量化写法，速度更快

这段代码要跑10个基因列表文件，帮我写一个批量处理的wrapper

这份代码目前是硬编码某个celltype，帮我改成for(cell in celltype_list)的通用循环版本
```

## 五、审稿/答辩应对类

```
这个reviewer质疑scTE的3'端偏好性，帮我构建一个多层次rebuttal（技术层面+验证层面+文献支持）

这段委员会/审稿意见，帮我列出关键质疑点，并逐条对应我已有的分析结果

这个数据，如果被质疑batch effect没控制好，我应该补充哪些验证图？
```

---

需要我针对某一类（比如"生信分析类"或"图注写作类"）再进一步细化，或者直接把这些整理成一份可以直接粘贴进您 `.sh` 文件的完整版清单吗？

---
---
---



# (1): Gemini

Jian Wu，看了你整理的这套 `AI 提示词 2-重要.sh`，你的提示词风格非常具有**实战导向**和**高效性**。它们大多属于“任务指引型”和“结构平移型”（例如：*“我的旧版 [ ]；但现在完全参考这重写 [ ]”*，这正是我们刚刚重写 GO 富集分析时的黄金公式，非常好用！）。

结合你日常在生物信息学（R/Python/Linux）**代码编写、数据分析以及**学术论文/结果撰写（Lung, Aging, 转座子）中的实际需求，我为你补充和精炼了一套“进阶版”AI 提示词。

这套提示词同样遵循你“言简意赅、直击要害”的风格，你可以直接复制到你的 `.sh` 或 `.txt` 提示词库中。

---

## 💻 一、 代码与算法类（R / Python / Linux）

在生信分析中，我们经常遇到内存暴涨、代码运行慢、或需要将 R 与 Python 互相转换的情况。以下提示词能帮你快速解决痛点：

### 1. 极简代码重构与性能优化（拒绝废话）

> **提示词：**
> "优化以下 [R/Python] 代码的运行效率和内存占用。保持输入输出逻辑完全不变，使用 [Seurat/data.table/dplyr/tidyverse] 惯用法，只输出优化后的代码，并在关键修改处附上极简注释。"

### 2. 跨语言无缝翻译（R ⇄ Python）

> **提示词：**
> "将以下 [R/Python] 代码翻译为对应的 [Python/R] 代码。确保矩阵/数据框的行列索引和维度完全对应，特别注意 [R 的 1-based / Python 的 0-based] 索引转换，并保持高效运行。"

### 3. 一键生成生信画图代码（ggplot2 / ComplexHeatmap）

> **提示词：**
> "使用 [ggplot2/ComplexHeatmap]，根据以下数据结构 [贴上 str() 或 head() 结果]，绘制一张 [火山图/热图/气泡图]。要求：1. 配色采用学术期刊常用高雅风格（如 Cell/Nature 风格）；2. 调整图例、轴标签及字体大小以符合发表标准；3. 提供完整可运行代码。"

### 4. 终端报错一键 Debug

> **提示词：**
> "我在 Linux 服务器上运行以下命令时报错：[贴上报错信息]。请结合我的代码 [贴上关键代码段]，分析导致该 Error/Warning 的最可能原因，并给出直接的修复代码。"

---

## 📝 二、 学术写作与模板迁移类（针对 Lung/Aging/转座子 领域）

你在写论文或项目进展时，需要将生信结果转化为地道的英文学术描述。以下提示词可以帮你在“准确性”和“高级感”之间找到完美平衡：

### 1. 生信结果一键生成“学术描述模板”（套用期刊风格）

> **提示词：**
> "我运行了 [分析名称，如：单细胞拟时序/单细胞转录组细胞类型鉴定]，得到了以下结果：[简要列出关键发现，如：发现 Epi 细胞在衰老过程中，ERV 表达显著上调]。
> 请参考顶级生物信息学期刊（如 Nature Methods, Genome Research）的语言风格，为我生成 3 个不同侧重点（如：强调生物学发现、强调计算方法严谨性、简练控字数）的英文结果描述模板（Results section）。"

### 2. 专有名词与逻辑审查

> **提示词：**
> "请润色以下学术段落。注意：
> 1. 保持生物信息学专业术语的精确性（如：TEs, ERVs, locus-resolved, multi-mapped reads 等）；
> 2. 优化句式，避免句首出现连续的 To + 动词不定式，增强句子间的因果与逻辑连贯性；
> 3. 提供修改后的版本以及核心修改点说明。"
> [贴上段落]
> 
> 

### 3. 极简图表 Legend 生成器

> **提示词：**
> "参考以下图表内容，为其撰写符合发表标准的 Figure Legend（图例说明）。
> 图表内容：[简要描述，如：展示了不同细胞类型在老化过程中激活的 HERVs 的气泡图，气泡大小代表基因数，颜色深浅代表 -log10(Padj)]。
> 格式参考：(A) Representative... [可贴上你喜欢的模板格式]"

---

## 🛠️ 三、 日常管理与效率类（Linux / Git / HPC）

### 1. 高性能计算（HPC）投递脚本生成（Slurm / SGE）

> **提示词：**
> "为以下任务编写一个 [Slurm/SGE] 作业投递脚本（.sh）：
> 运行命令：[你的运行命令，如 Rscript run_scTE.R]
> 要求：设置限制为 16 核 CPU、128G 内存、最大运行时间 48 小时，任务命名为 [项目名]，并在运行结束或失败时发送邮件到我的邮箱。"

### 2. Git 极简 Commit Message 生成

> **提示词：**
> "根据我以下的代码修改内容，生成一条符合 Angular 规范（如 feat:, fix:, refactor:, docs:）的、简明扼要的英文 Git commit message："
> [贴上 git diff 摘要或简述修改了什么]

---

### 💡 提示词保存与管理建议：

你目前把它们保存在 `.sh` 脚本中（比如通过 `cat`、`echo` 或者别名调用），这非常符合 Linux 用户的习惯。

* 你可以考虑在 `.bashrc` 或 `.zshrc` 中为最常用的提示词设置 **Alias（别名）**。
* 例如：`alias prompt-rewrite="cat ~/Desktop/AI提示词2-重要.sh | grep -A 2 '我的旧版'"`，这样在终端里随时能快速复制和查阅！

---
---


# (3) ChatGPT
看了你的截图，你目前整理的提示词偏向 **"代码迁移、代码补全、详细注释"**，这只是 AI 在科研中的一小部分。

根据你的研究方向（生物信息学、R/Python、单细胞、转座子、论文写作），我建议建立一个**科研 AI Prompt Library**，以后几乎所有工作都能直接复制粘贴。

---

# ① Coding（代码）

## 完整重写（你已经有）

```text
完全参考我的旧版框架，从头重新编写。

要求：
- 不遗漏任何功能
- 保留所有变量名
- 增加详细英文注释
- 提高代码可读性
- 不允许省略任何代码
```

---

## 自动检查Bug

```text
请不要直接修改。

请先检查整个代码。

列出：
1. Bug
2. 潜在Bug
3. 性能问题
4. 内存问题
5. 可读性问题

然后再修改。
```

---

## 提高运行速度

```text
请不要改变任何分析结果。

目标：
仅优化运行速度。

尽可能减少：
- for循环
- 重复计算
- 内存占用
```

---

## Seurat优化

```text
请检查整个Seurat流程。

重点：

是否符合Seurat v5推荐流程？

是否存在重复Normalize？

是否存在重复ScaleData？

是否可以减少RAM？
```

---

## Linux脚本优化

```text
请把整个Shell脚本改成更加稳定版本。

要求：

set -euo pipefail

所有变量增加双引号

增加错误检测

增加日志输出

增加时间统计
```

---

# ② Debug

这个特别好用。

```text
不要猜。

一步一步分析为什么报错。

告诉我：

① 根本原因

② 为什么发生

③ 如何修复

④ 是否还有隐藏问题
```

---

## 看Log

```text
根据Log定位真正错误。

不要只解释最后一句。

分析整个调用过程。
```

---

# ③ Figure（论文画图）

这个你以后天天会用。

```text
请从Nature论文角度评价这张图。

评分：

美观

逻辑

颜色

字体

布局

信息量

是否达到Nature标准

最后给修改建议。
```

---

## 修改Figure

```text
保持所有数据不变。

仅优化：

颜色

字体

Legend

Label

Spacing

Panel顺序
```

---

## Figure逻辑

```text
请重新设计Figure Story。

目标：

Figure之间具有故事线。

每张Figure回答一个科学问题。
```

---

# ④ Paper Writing

这是你最需要的。

---

## SCI润色

```text
不要改变科学意思。

按照Nature Cell格式润色。

提高：

逻辑

可读性

学术性

不要增加没有证据的话。
```

---

## Results

```text
按照Nature Results写法。

要求：

第一句总结发现

第二句解释

第三句引用Figure

第四句总结意义
```

---

## Discussion

```text
不要重复Results。

重点：

为什么重要？

为什么以前没人发现？

未来意义？

局限性？
```

---

# ⑤ Reviewer模式（超级推荐）

```text
假设你是Nature Reviewer。

请找出：

所有漏洞

所有逻辑问题

所有统计问题

所有Figure问题

所有实验不足

越严格越好。
```

---

## Editor模式

```text
假设你是Nature Editor。

你会拒稿吗？

为什么？

需要补哪些内容？
```

---

# ⑥ Research（科研设计）

```text
根据目前结果。

提出：

5个新的Hypothesis

按照创新性排序。
```

---

## 下一步实验

```text
如果你是PI。

下一步会做哪些实验？

按照：

必要

推荐

可选

三个等级。
```

---

# ⑦ 单细胞分析（你应该建立）

```text
检查整个单细胞分析流程。

包括：

QC

Normalization

Integration

Cluster

Annotation

DEG

Trajectory

Cell-cell

Scoring

是否符合目前最佳实践？
```

---

## TE分析

```text
假设你是TE领域专家。

评价整个TE分析。

是否遗漏：

ERV

LINE

SINE

LTR

Satellite

Family

Subfamily

Locus-level分析？
```

---

# ⑧ Website

```text
请从PI招聘学生角度评价我的主页。

评分：

专业度

科研水平

设计

导航

第一印象

最后提出修改建议。
```

---

# ⑨ 项目管理

```text
根据整个项目。

列出：

必须完成

建议完成

以后完成

按照优先级排序。
```

---

# ⑩ Prompt（我最推荐建立）

以后可以直接要求AI：

```text
请先思考，再回答。

不要急着输出。

如果信息不足，请先问我。

如果有多种方案，请全部列出来并比较。

最后推荐最佳方案，并解释原因。
```

---

# ⑪ 针对你最值得收藏的几个 Prompt

结合你的研究方向（单细胞 + 转座子 + Atlas + Nature论文），我认为使用频率最高的是：

1. **代码完整迁移（禁止省略）**
2. **Nature Reviewer 模式**
3. **Nature Editor 模式**
4. **Figure Story 重构**
5. **Results 重写（Nature风格）**
6. **Discussion 深度提升**
7. **单细胞分析流程审查（Best Practice）**
8. **转座子 专家审查（遗漏检查）**
9. **R/Python/Seurat 性能优化（不改变结果）**
10. **科研下一步规划（PI视角）**
11. **论文逻辑一致性检查（标题 → Figure → Results → Discussion）**
12. **项目优先级规划（按发表价值、创新性和工作量排序）**

**建议**：把这些提示词按 **Coding、Debug、Writing、Figures、Research、Review、Project Management** 七大类整理成一个 Markdown 或 Obsidian 知识库，每类再细分常用模板。这样不仅便于复制使用，也方便随着研究积累不断扩展，长期来看会成为一个非常高效的个人科研 AI 工具库。


---




